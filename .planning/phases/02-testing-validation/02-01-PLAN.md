---
phase: 02-testing-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [test_grok_direct.py]
autonomous: true
requirements: [TEST-01, TEST-03, NFR-01]

must_haves:
  truths:
    - "Direct testing of Grok response generation works."
    - "Latency for Grok model is measured and reported."
    - "Follow-up question generation is tested independently."
  artifacts:
    - path: "test_grok_direct.py"
      provides: "Direct function testing and benchmarking"
  key_links:
    - from: "test_grok_direct.py"
      to: "lambda/lambda_function.py"
      via: "importing generate_grok_response, generate_followup_questions"
---

<objective>
Create a new script `test_grok_direct.py` to test the core logic functions (`generate_grok_response` and `generate_followup_questions`) directly, bypassing the Alexa handler. This will allow for faster iteration and direct performance measurement.

Purpose: Faster iteration and performance benchmarking.
Output: `test_grok_direct.py` script.
</objective>

<execution_context>
@/Users/romain.vandooren/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/romain.vandooren/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lambda/lambda_function.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_grok_direct.py</name>
  <files>test_grok_direct.py</files>
  <action>
    Create a new Python script that imports the core logic functions from `lambda/lambda_function.py`.
    The script should:
    - Require `XAI_API_KEY` to be set in the environment.
    - Test `generate_grok_response` with various queries and check the output format.
    - Test `generate_followup_questions` with sample conversation history and check that it returns a list of short questions.
    - Include error handling for missing API keys or network issues.
  </action>
  <verify>
    <automated>XAI_API_KEY=test python3 test_grok_direct.py</automated>
  </verify>
  <done>Script created and successfully imports/calls the core functions (even if it fails with a real API error if the key is dummy).</done>
</task>

<task type="auto">
  <name>Task 2: Implement latency measurement and content validation</name>
  <files>test_grok_direct.py</files>
  <action>
    Enhance `test_grok_direct.py` to:
    - Use the `time` module to measure the execution time of API calls.
    - Print the latency for each call.
    - Verify that `generate_grok_response` returns a tuple (response_text, followup_questions).
    - Validate that the response text is within a reasonable length (roughly matching the system prompt's 50-word limit).
    - Verify that the model `grok-4-1-fast-non-reasoning` returns results in less than 5 seconds.
  </action>
  <verify>
    <automated>XAI_API_KEY=test python3 test_grok_direct.py</automated>
  </verify>
  <done>Performance measurement and validation logic is implemented in the script.</done>
</task>

</tasks>

<verification>
Check that `test_grok_direct.py` exists and correctly benchmarks the core functions.
</verification>

<success_criteria>
- `test_grok_direct.py` script is fully functional.
- Core logic is tested independently of Alexa.
- Latency measurements are implemented and provide clear feedback on the 5-second target.
</success_criteria>

<output>
After completion, create `.planning/phases/02-testing-validation/02-01-SUMMARY.md`
</output>
