---
phase: 02-testing-validation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [test_lambda.py]
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]

must_haves:
  truths:
    - "Multi-turn session testing works in test_lambda.py."
    - "Session attributes (chat history) are preserved across turns."
    - "Follow-up questions are correctly extracted from SSML output."
  artifacts:
    - path: "test_lambda.py"
      provides: "Full Alexa-Lambda integration testing with state"
  key_links:
    - from: "test_lambda.py"
      to: "lambda/lambda_function.py"
      via: "lambda_handler call"
---

<objective>
Enhance `test_lambda.py` to support multi-turn session testing and follow-up question verification. This will simulate real-world Alexa usage where state is maintained across multiple user queries.

Purpose: Full integration testing with session persistence.
Output: Updated `test_lambda.py`.
</objective>

<execution_context>
@/Users/romain.vandooren/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/romain.vandooren/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lambda/lambda_function.py
@test_lambda.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement multi-turn session persistence in test_lambda.py</name>
  <files>test_lambda.py</files>
  <action>
    Modify `test_lambda.py` to:
    - Update `create_alexa_request` to accept optional `session_attributes`.
    - Create a new test function `test_multi_turn_session(queries)` that:
        - Starts with an empty session.
        - For each query in `queries`, calls `lambda_handler` with the current session attributes.
        - Updates the session attributes with the ones returned in the response.
        - Verifies that `chat_history` grows with each turn.
    - Implement command-line support to trigger this multi-turn test.
  </action>
  <verify>
    <automated>python3 test_lambda.py --session</automated>
  </verify>
  <done>Multi-turn session testing is implemented and correctly maintains state across calls.</done>
</task>

<task type="auto">
  <name>Task 2: Add follow-up extraction and SSML verification</name>
  <files>test_lambda.py</files>
  <action>
    Enhance the verification logic in `test_lambda.py` to:
    - Parse the SSML `speak` output and look for the "You could ask: " pattern.
    - Extract the suggested follow-up questions from the speech.
    - Verify that these questions match the ones stored in `sessionAttributes['followup_questions']`.
    - Verify that Grok-specific mentions (like "Grok mode") are present in the responses.
    - Ensure that the reprompt text is correctly formatted.
  </action>
  <verify>
    <automated>python3 test_lambda.py</automated>
  </verify>
  <done>Verification logic is enhanced to check follow-up suggestions and SSML output format.</done>
</task>

</tasks>

<verification>
Check that `test_lambda.py` correctly handles sessions and verifies follow-up questions in the output speech.
</verification>

<success_criteria>
- `test_lambda.py` supports multi-turn conversations.
- Session persistence is verified (e.g., answering "who is he?" in context).
- Follow-up question extraction from SSML is functional.
</success_criteria>

<output>
After completion, create `.planning/phases/02-testing-validation/02-02-SUMMARY.md`
</output>
