---
phase: 01-api-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [lambda/lambda_function.py]
autonomous: true
requirements: [FUN-02, FUN-05, NFR-03]
user_setup:
  - service: xAI
    why: "Access to Grok API"
    env_vars:
      - name: XAI_API_KEY
        source: "xAI Console -> API Keys"

must_haves:
  truths:
    - "The Lambda function uses the xAI base URL for all API calls"
    - "The model name is defined as a central variable and uses 'grok-4-1-fast-non-reasoning'"
    - "The API key can be loaded from an environment variable"
  artifacts:
    - path: "lambda/lambda_function.py"
      provides: "Updated xAI integration"
  key_links:
    - from: "lambda/lambda_function.py"
      to: "https://api.x.ai/v1/chat/completions"
      via: "requests.post"
---

<objective>
Migrate the Lambda function to use xAI's Grok API. This involves updating the endpoint, model name, and refactoring configuration for better maintainability and security.

Purpose: Replace OpenAI with xAI and ensure consistency across primary response and follow-up question generation.
Output: Updated `lambda/lambda_function.py` pointing to xAI.
</objective>

<execution_context>
@/Users/romain.vandooren/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/romain.vandooren/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/RESEARCH.md
@lambda/lambda_function.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize Configuration and Update Meta-information</name>
  <files>lambda/lambda_function.py</files>
  <action>
    - Import `os` module.
    - Define global constants at the top of the file:
      - `XAI_API_KEY = os.environ.get("XAI_API_KEY", "YOUR_API_KEY")`
      - `XAI_MODEL = "grok-4-1-fast-non-reasoning"`
      - `XAI_BASE_URL = "https://api.x.ai/v1"`
    - Replace `api_key` and `model` variables with these new constants.
    - Update `LaunchRequestHandler` speak_output from "Chat G.P.T. mode activated" to "Grok mode activated".
    - Update `CancelOrStopIntentHandler` speak_output from "Leaving Chat G.P.T. mode" to "Leaving Grok mode".
  </action>
  <verify>
    <automated>grep -E "XAI_API_KEY|XAI_MODEL|XAI_BASE_URL" lambda/lambda_function.py</automated>
  </verify>
  <done>Configuration centralized and Alexa response text updated to reflect the new AI.</done>
</task>

<task type="auto">
  <name>Task 2: Update API Functions to use xAI Endpoint</name>
  <files>lambda/lambda_function.py</files>
  <action>
    - Refactor `generate_followup_questions` to use `XAI_BASE_URL + "/chat/completions"` and `XAI_MODEL`.
    - Refactor `generate_gpt_response` (consider renaming to `generate_grok_response` for consistency) to use `XAI_BASE_URL + "/chat/completions"` and `XAI_MODEL`.
    - Ensure `headers` use `XAI_API_KEY`.
    - Verify that both functions use the `XAI_MODEL` constant instead of hardcoded strings like "gpt-3.5-turbo" or "gpt-4o-mini".
    - Update any remaining OpenAI references in variable names or comments to xAI/Grok.
  </action>
  <verify>
    <automated>grep -v "api.openai.com" lambda/lambda_function.py | grep "api.x.ai"</automated>
  </verify>
  <done>Both functions updated to point to xAI endpoint and use the correct model.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Response Handling and Intent Callers</name>
  <files>lambda/lambda_function.py</files>
  <action>
    - If `generate_gpt_response` was renamed, update the call site in `GptQueryIntentHandler.handle`.
    - Ensure `max_completion_tokens` is used consistently as `max_completion_tokens` (xAI supports this OpenAI parameter).
    - Double check that `is_followup` logic remains compatible with xAI responses.
  </action>
  <verify>
    <automated>python3 -c "import sys; sys.path.append('lambda'); import lambda_function; print('Successfully imported lambda_function')"</automated>
  </verify>
  <done>Intent handler correctly calls the updated response generator and the code is syntactically valid.</done>
</task>

</tasks>

<verification>
Check for any remaining hardcoded OpenAI endpoints or model names.
Verify that the API key is retrieved from environment variables with a fallback for backward compatibility.
</verification>

<success_criteria>
- No occurrences of `api.openai.com` in `lambda/lambda_function.py`.
- No occurrences of `gpt-` models in `lambda/lambda_function.py`.
- `XAI_API_KEY`, `XAI_MODEL`, and `XAI_BASE_URL` are defined and used.
- `lambda/lambda_function.py` is valid Python and can be imported.
</success_criteria>

<output>
After completion, create `.planning/phases/01-api-integration/01-01-SUMMARY.md`
</output>
