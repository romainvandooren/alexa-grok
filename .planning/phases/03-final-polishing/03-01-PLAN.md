---
phase: 03-final-polishing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [skill.json, lambda/lambda_function.py, test_lambda.py, test_grok_direct.py]
autonomous: true
requirements: [FUN-01, FUN-05, NFR-02]

must_haves:
  truths:
    - "skill.json contains no 'ChatGPT' or 'OpenAI' references in any locale"
    - "lambda/lambda_function.py uses GrokQueryIntentHandler class name"
    - "lambda/lambda_function.py contains no OpenAI/GPT comments"
    - "Tests run and pass after refactoring"
  artifacts:
    - path: "skill.json"
      provides: "Updated skill manifest with Grok branding"
    - path: "lambda/lambda_function.py"
      provides: "Renamed handler and cleaned comments"
  key_links:
    - from: "lambda/lambda_function.py"
      to: "sb.add_request_handler"
      via: "GrokQueryIntentHandler registration"
---

<objective>
Update the skill configuration and refactor the Lambda function to complete the branding migration to Grok/xAI. This includes updating all 15 locales in skill.json and renaming the primary intent handler in the code.

Purpose: Consistency and professional branding across all skill components.
Output: Updated `skill.json`, `lambda/lambda_function.py`, and test scripts.
</objective>

<execution_context>
@/Users/romain.vandooren/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/romain.vandooren/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@skill.json
@lambda/lambda_function.py
@test_lambda.py
@test_grok_direct.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update skill.json branding in all locales</name>
  <files>skill.json</files>
  <action>
    - Replace all occurrences of "ChatGPT" with "Grok" in `name`, `summary`, and `description` across all 15 locales (en-US, en-GB, en-CA, en-IN, en-AU, es-ES, es-MX, es-US, fr-FR, fr-CA, ja-JP, de-DE, it-IT, hi-IN, pt-BR).
    - Replace all occurrences of "OpenAI" with "xAI" in all `description` fields.
    - Ensure the "en-US" locale uses "Grok" in summary and description.
  </action>
  <verify>
    <automated>! grep -iE "chatgpt|openai" skill.json</automated>
  </verify>
  <done>skill.json is fully updated with Grok and xAI branding in all locales.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor lambda_function.py for Grok branding</name>
  <files>lambda/lambda_function.py</files>
  <action>
    - Rename the class `GptQueryIntentHandler` to `GrokQueryIntentHandler`.
    - Update the `sb.add_request_handler(GptQueryIntentHandler())` call to `sb.add_request_handler(GrokQueryIntentHandler())`.
    - Update the docstring for `GrokQueryIntentHandler` to "Handler for Grok Query Intent."
    - Scan for and remove ALL remaining OpenAI or GPT specific comments (e.g., "# Set your OpenAI API key").
    - Ensure `XAI_API_KEY` default value is explicitly "YOUR_API_KEY" or "PLACEHOLDER".
  </action>
  <verify>
    <automated>! grep -iE "openai|gpt" lambda/lambda_function.py | grep "#"</automated>
  </verify>
  <done>lambda_function.py is refactored with Grok naming and no legacy comments.</done>
</task>

<task type="auto">
  <name>Task 3: Update Tests and Verify</name>
  <files>test_lambda.py, test_grok_direct.py</files>
  <action>
    - In `test_lambda.py`:
      - Update the header docstring to mention "Alexa Grok" instead of "Alexa GPT".
      - Ensure `GptQueryIntent` usage (in request creation) remains as is for Alexa compatibility, but add a comment explaining this.
      - Update any other comments that still refer to GPT.
    - In `test_grok_direct.py`:
      - Clean up any remaining OpenAI/GPT references in comments or docstrings.
    - Run both tests to ensure they still pass.
  </action>
  <verify>
    <automated>python3 test_lambda.py && python3 test_grok_direct.py</automated>
  </verify>
  <done>Test scripts are updated and pass verification.</done>
</task>

</tasks>

<verification>
Check skill.json for any missed locales.
Verify lambda_function.py still runs by importing it in a test script.
</verification>

<success_criteria>
- `skill.json` contains no legacy branding.
- `lambda/lambda_function.py` is clean and correctly registered.
- `test_lambda.py` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-final-polishing/03-01-SUMMARY.md`
</output>
